services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: manuv
      POSTGRES_PASSWORD: manuvpass
      POSTGRES_DB: manuvdb
    ports:
      - "5432:5432"
    volumes:
      - manuv_pg:/var/lib/postgresql/data

volumes:
  manuv_pg: